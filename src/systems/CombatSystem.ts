import {Subject} from "../entities/Subject";import {Greed} from "../entities/Greed";import type {GameModel} from "../core/types";
export class CombatSystem{cooldowns=new Map<Subject,number>();
update(model:GameModel,dt:number,subjects:Subject[],enemies:Greed[],castleX:number,onKill:(e:Greed)=>void,onCastleHit:(d:number)=>void){for(const s of subjects){if(!["guard","archer","royalGuard"].includes(s.kind))continue;let best:Greed|undefined;let bd=Infinity;for(const e of enemies){const d=Math.abs(e.x-s.x);if(d<bd&&d<330){bd=d;best=e}}if(best){s.targetX=best.x;if(bd<(s.kind==="archer"?220:58)){const cd=this.cooldowns.get(s)||0;if(cd<=0){this.cooldowns.set(s,s.kind==="archer"?1.1:.72);best.hp-=s.kind==="royalGuard"?14:s.kind==="guard"?9:7;if(best.hp<=0){onKill(best);best.destroy()}}}}else if(model.running)s.targetX=model.playerX+(s.kind==="royalGuard"?70:Math.sin(model.time*.6+s.x)*100)}
for(const e of [...enemies]){if(e.destroyed)continue;const target=enemies.length?this.nearestGuard(e,subjects):undefined;const targetX=target?target.x:castleX;if(Math.abs(e.x-targetX)<54&&target){target.hp=(target.hp??30)-(e.brute?10:5);if((target.hp??1)<=0){target.destroy()}}else e.update(dt,targetX)}
this.cooldowns.forEach((v,k)=>this.cooldowns.set(k,Math.max(0,v-dt)));
for(const e of enemies){if(e.destroyed)continue;if(Math.abs(e.x-castleX)<48){onCastleHit(e.brute?12:6);e.destroy()}}}
private nearestGuard(e:Greed,subjects:Subject[]){let best:Subject|undefined,bd=Infinity;for(const s of subjects){if(!["guard","archer","royalGuard"].includes(s.kind))continue;const d=Math.abs(s.x-e.x);if(d<bd&&d<210){bd=d;best=s}}return best}}