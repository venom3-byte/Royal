import {EnemyState,PlayerState,UnitState} from "../core/Types";export class CombatSystem{cooldown=0;update(dt:number,p:PlayerState,units:UnitState[],enemies:EnemyState[]){this.cooldown=Math.max(0,this.cooldown-dt);for(const u of units)for(const e of enemies)if(Math.abs(u.x-e.x)<80)e.hp-=dt*(u.role==="archer"?8:5);for(const e of enemies)if(Math.abs(e.x-p.x)<48&&this.cooldown===0){p.health-=4;this.cooldown=.6}}}