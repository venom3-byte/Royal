import type {GameModel} from "../core/types";export class EconomySystem{update(m:GameModel,dt:number){if(!m.running||m.gameOver)return;m.gold+=dt*.035;m.food=Math.max(0,m.food-m.population*dt*.006);if(m.food===0)m.notice="المؤن نفدت.";if(Math.floor(m.time)%30===0)m.wood+=dt*.12}build(m:GameModel,cost:{gold:number;wood:number;stone:number},apply:()=>void){if(m.gold<cost.gold||m.wood<cost.wood||m.stone<cost.stone){m.notice="الموارد غير كافية.";return false}m.gold-=cost.gold;m.wood-=cost.wood;m.stone-=cost.stone;apply();return true}}