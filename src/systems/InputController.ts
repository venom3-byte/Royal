import Phaser from "phaser";export class InputController{moveX=0;attack=false;coin=false;keys:any;constructor(public scene:Phaser.Scene){scene.input.addPointer(2);this.keys=scene.input.keyboard?.addKeys("A,D,LEFT,RIGHT,SPACE") as any;scene.input.on("pointerdown",(p:Phaser.Input.Pointer)=>{if(p.x>scene.scale.width*.72&&p.y>scene.scale.height*.65)this.attack=true;else if(p.x>scene.scale.width*.45&&p.y>scene.scale.height*.65)this.coin=true});scene.input.on("pointermove",(p:Phaser.Input.Pointer)=>{if(p.isDown&&p.x<scene.scale.width*.4){const cx=scene.scale.width*.16;this.moveX=Phaser.Math.Clamp((p.x-cx)/70,-1,1)}});scene.input.on("pointerup",(p:Phaser.Input.Pointer)=>{if(p.x<scene.scale.width*.4)this.moveX=0})}update(){if(this.keys){const l=this.keys.A.isDown||this.keys.LEFT.isDown,r=this.keys.D.isDown||this.keys.RIGHT.isDown;this.moveX=(r?1:0)-(l?1:0);if(this.keys.SPACE.isDown)this.attack=true}}consumeAttack(){const v=this.attack;this.attack=false;return v}consumeCoin(){const v=this.coin;this.coin=false;return v}}