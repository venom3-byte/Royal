import {DAY_LENGTH,QUALITY,WEAPONS} from "../config.js";import {emit} from "../core/events.js";import {updateTime,isNight,seasonData,currentPhase} from "../core/time.js";import {updatePlayer} from "../entities/player.js";import {updateUnits,promoteUnits,removeDeadUnits,guardSpeed} from "../entities/units.js";import {updateAnimals,cullHungryAnimals} from "../entities/animals.js";import {updateFarming} from "./farming.js";import {updateEconomy} from "./economy.js";import {updateWagon} from "./wagon.js";import {updateEnemies,spawnNightWave} from "../entities/enemy.js";import {updateProjectiles} from "./combat.js";
export {build,upgradeCastle} from "./buildings.js";export {fireCannon,updateProjectiles,attackUnit} from "./combat.js";export {harvestNearest} from "./farming.js";export {boardWagon} from "./wagon.js";export {updateTime,isNight,seasonData,currentPhase};
export function tick(state,dt,api){if(!state.running||state.paused||state.gameOver)return;const phaseBefore=isNight(state),chg=updateTime(state,dt);state.seasonGrowth=seasonData(state).growth;state.weatherGrowth={clear:1,rain:1.15,fog:1.02,wind:.95,snow:.55,heat:.82}[state.weather]||1;const phaseAfter=isNight(state);if(!phaseBefore&&phaseAfter)spawnNightWave(state,api.makeEnemy,api.notify);if(chg.dayChanged)promoteUnits(state);updateEconomy(state,dt,chg.dayChanged);updateFarming(state,dt);updateAnimals(state,dt);cullHungryAnimals(state);removeDeadUnits(state,api.notify);updateUnits(state,dt,{...api,isNight:()=>phaseAfter,guardSpeed,attackUnit:(u,t)=>api.attackUnit(u,t)});updateEnemies(state,dt,{...api,seasonNightMultiplier:()=>seasonData(state).night});updateProjectiles(state,dt,api);updateWagon(state,dt);updatePlayer(state,dt);if(state.castle.hp<=0)state.gameOver=true}
