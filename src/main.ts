import * as Phaser from "phaser";import "../assets/rsk_assets.js";import "./styles.css";import {BootScene} from "./scenes/BootScene";import {WorldScene} from "./scenes/WorldScene";import {GAME_HEIGHT,GAME_WIDTH} from "./core/constants";
const root=document.getElementById("game-root")!,errorBox=document.getElementById("boot-error")!;const report=(title:string,e:unknown)=>{errorBox.hidden=false;errorBox.textContent=title+"\n"+String((e as any)?.stack||e)};window.addEventListener("error",e=>report("Game Error",e.error||e.message));window.addEventListener("unhandledrejection",e=>report("Promise Error",e.reason));
const config:Phaser.Types.Core.GameConfig={type:Phaser.AUTO,parent:root,width:GAME_WIDTH,height:GAME_HEIGHT,backgroundColor:"#09131c",pixelArt:true,antialias:false,roundPixels:true,scale:{mode:Phaser.Scale.RESIZE,width:GAME_WIDTH,height:GAME_HEIGHT,autoCenter:Phaser.Scale.CENTER_BOTH},physics:{default:"arcade",arcade:{gravity:{x:0,y:0},debug:false,fixedStep:true,fps:60}},scene:[BootScene,WorldScene],render:{powerPreference:"high-performance"},banner:false};
new Phaser.Game(config);