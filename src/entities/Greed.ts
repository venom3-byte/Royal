import * as Phaser from "phaser";export class Greed extends Phaser.GameObjects.Container{hp:number;speed:number;brute:boolean;constructor(scene:Phaser.Scene,x:number,y:number,brute:boolean){super(scene,x,y);this.brute=brute;this.hp=brute?45:18;this.speed=brute?24:38;scene.add.existing(this);this.setDepth(28);const g=scene.add.graphics();g.fillStyle(brute?0x4f2431:0x2d2037,1);g.fillEllipse(0,-25,brute?34:26,brute?44:35);g.fillStyle(0xd85b67,1);g.fillCircle(-6,-42,3);g.fillCircle(6,-42,3);g.fillStyle(0x141018,1);g.fillRect(-8,-10,16,10);this.add(g)}
update(dt:number,targetX:number){this.x+=Math.sign(targetX-this.x)*this.speed*dt}}