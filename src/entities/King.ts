import * as Phaser from "phaser";export class King extends Phaser.Physics.Arcade.Sprite{constructor(scene:Phaser.Scene,x:number,y:number){super(scene,x,y,"king.png");scene.add.existing(this);scene.physics.add.existing(this);this.setDepth(30);this.body.setAllowGravity(false);this.body.setDragX(1200);this.body.setMaxVelocity(200,0);this.setCollideWorldBounds(true)}move(dir:number){this.setVelocityX(dir*155);if(dir)this.setFlipX(dir<0)}stop(){this.setVelocityX(0)}update(model:any){model.playerX=this.x;model.playerVx=this.body.velocity.x;model.playerFacing=this.flipX?-1:1}}